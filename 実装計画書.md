# Pharmakon 実装計画書

**作成日**: 2026-05-11
**ベース**: Phase 0–8 完了 + コード品質改善 Phase 1–5 完了 + P0/P1 修正完了
**総コード規模**: ~13,600行 (Rust), 45+ファイル, 7 crate

---

## 0. プロジェクト現状

### アーキテクチャ概要

```
pharmakon-cli       # CLI / TUI エントリポイント
pharmakon-gateway   # REST API / WebSocket / Telegram/Discord/Slack チャネル
pharmakon-core      # Agentループ / モデルルーティング / entropy監視 / CodeAct / DSGE
pharmakon-tools     # 65+ツール (shell, code, browser, search, codex, ...)
pharmakon-memory    # KnowledgeNexus (LanceDB) / SQLiteグラフ / 因果DAG
pharmakon-common    # 共有型 (Event, AgentError, SpawnHandle, ...)
pharmakon-plugin-sdk # プラグインSDK
```

### 完了済みフェーズ

| Phase | 内容 | 状態 |
|---|---|---|
| 0 | Foundation: ToolMetaRegistry, EventLog, SnapshotStore | ✅ |
| 1 | Control Plane: Entropy Monitor, Rollback, Scheduler | ✅ |
| 2 | Intelligence: Capability Abstraction, Causal Memory, Swarm | ✅ |
| 3 | Advanced: CodeAct, Constitutional Engine, Durable Tasks | ✅ |
| 4 | Self-Evolving: Skill Genome, Dream Mode, Model Router, DSGE | ✅ |
| 5 | World Model: World Model Agent, Dynamic max_tokens, Codex | ✅ |
| 6 | Robustification: World Model V2, Constraint Validation, Bayesian | ✅ |
| 7 | Cognitive Compiler: PlanCompiler, Skill Compression, Self-Healing | ✅ |
| 8 | Cognitive Runtime: Speculative Exec, Token Economy v2, Pattern Mining, AOT, Causal Memory, Fabric, Benchmarks | ✅ |

### 直近のコード品質改善（Phase 1–5 / P0–P1）

| # | 内容 | ファイル |
|---|---|---|
| A1 | テスト復活: agent_test.rs コメントアウト解除・追従修正 | `crates/core/tests/agent_test.rs` |
| A2 | `try_lock` + `expect` パニック防止（遅延評価化） | `crates/core/src/agent.rs` |
| A3 | Agent Clone 状態共有: economy/skill_library/dream_started を Arc 共有化 | `crates/core/src/agent.rs` |
| A4 | chat_on_session 分割: 1250→893行 (29%減) + 6メソッド抽出 | `crates/core/src/agent.rs` |
| A5 | エラーハンドリング改善: 7箇所の `let _ =` を `log::warn!` / `?` 化 | `crates/core/src/agent.rs` |
| A6 | `/model` 副作用除去: Dream Mode起動を明示的メソッドに分離 | `crates/core/src/agent.rs` |
| A7 | 未使用依存削除: qdrant-client 削除 + InMemoryVectorStore | `Cargo.toml` / `vector_store.rs` |
| A8 | ModelRouter抽出: モデル選択/フォールバック/economy連携を別ファイル化 | `crates/core/src/model_router.rs` |
| P0 | 応答消失: FixedIterations終了時のfallback改善 | `crates/core/src/agent.rs` |
| P1 | リアルタイム情報: 時刻/日付クエリ自動検出＋date実行＋コンテキスト注入 | `crates/core/src/agent.rs` |

### 検証ステータス

```bash
cargo test --workspace   → 89 passed, 0 failed, 2 ignored (Ollama)
cargo clippy --workspace --all-targets → pharmakon-core 警告ゼロ
```

---

## 0.5 即時修正が必要なクリティカル問題

`crates/core/src/orchestration/world.rs`（1305行）のレビュー結果から、データ損失リスクを含む問題を最優先で修正する。

### CRITICAL: `git clean -fd` によるデータ損失リスク

**現状**: `execute_world_model()` 内で、プラン実行失敗時に以下の処理が無条件実行される（L1194-1203）:

```rust
// 全変更を破棄
std::process::Command::new("git").args(["checkout", "."]).current_dir(&workspace_root).output().ok();
// 全未追跡ファイルを削除！ ← データ損失
std::process::Command::new("git").args(["clean", "-fd"]).current_dir(&workspace_root).output().ok();
```

**影響**: ユーザーの重要な未追跡ファイルやステージング中の変更が永久消失する。

**対策**:
- SnapshotStore ベースのロールバック（既に実装済み、L1187-1191）のみ使用する
- `git clean -fd` と `git checkout .` は削除し、代わりに `git stash --include-untracked` を使う（復元可能）
- または ConstitutionalPolicy で `git clean -fd` を禁止コマンドに追加

**ファイル**: `crates/core/src/orchestration/world.rs`, `crates/core/src/security/policy.rs`
**見積もり**: +30行, 30分

### HIGH: StaticVerifier のリスク値が ExecutionProfile と重複

**現状**: ツールごとのリスク値がマジックナンバーで定義されているが、`pharmakon-common` に既存の `ExecutionProfile`（SideEffectLevel / FilesystemScope / Reversibility）がある。重複を解消し、一貫したリスク評価に統一する。

**ファイル**: `crates/core/src/orchestration/world.rs`
**見積もり**: +50行, 1時間

### MEDIUM: Bayesian Scoring の楽観的バイアス

**現状**: スキルライブラリに実績がない場合の事前確率が `0.8`（高すぎ）。Beta(1,1) の無情報事前分布（期待値0.5）に変更する。

**ファイル**: `crates/core/src/orchestration/world.rs`
**見積もり**: +10行, 10分

### MEDIUM: Conditional ノードの条件式が型安全でない

**現状**: `condition_script: String` が Rhai スクリプト文字列。実行時エラーになりやすく、`"true"` 文字列一致で分岐する暗黙的プロトコルになっている。

**改善案**: `enum Condition { FileExists(PathBuf), CargoCheckSuccess, Script(String) }` に型付け。

**ファイル**: `crates/core/src/orchestration/world.rs`
**見積もり**: +80行, 1時間

### LOW: tokio::spawn のエラー握りつぶし

**現状**: `PlanCache::record_result()` 内のパターンマイニング（L421-433）が `tokio::spawn` で投げっぱなし。内部パニック時に静かに消える。

**ファイル**: `crates/core/src/orchestration/world.rs`
**見積もり**: +10行, 10分

**コアテーゼ**: Phase 8 で「プランが実行時に自らを最適化するランタイム」を得た。Phase 9 では「コードベース自体が自己改善するフィードバックループ」に入る。

**期間目標**: 2026年6月末
**成功指標**:
- コード生成の成功率 > 90%（cargo check通過率）
- 自己導入した最適化による実行時性能改善 > 20%
- 人間のコードレビューなしでマージ可能なPR率 > 50%

---

### 9-1: 自己コード修正の安全基盤

**現状**: ConstitutionalEngine は「ソースコードの改変禁止」ルールを持つ。自己修正を可能にするには、このルールを解除する代わりに「検証済み変更のみ許可」に置き換える必要がある。

```rust
// 現状 (security/policy.rs): ハードコードされた禁止パス
if path_lower.contains("crates/core/src/") { Deny(...) }

// 目標: 署名付き変更のみ許可
enum SelfModificationPolicy {
    ConstitutionallyBanned,      // 今まで通り禁止
    VerifiedOnly,                // tests + clippy通過が条件
    Audited { auditor_id: String }, // 許可リストによる制御
}
```

**実装項目**:
- [ ] `SelfModificationPolicy` enum 追加
- [ ] ConstitutionalPolicy の自己修正ルールを条件付き許可に変更
- [ ] `verify_and_rollback()` — cargo check + test を自動実行し、失敗時に自動ロールバック
- [ ] CLI: `/self-mod enable` / `/self-mod disable` コマンド

**ファイル**: `crates/core/src/security/policy.rs`, `crates/core/src/security/mod.rs`
**見積もり**: +200行, 2日

---

### 9-2: コード生成・検証パイプライン

**現状**: CodeActでスクリプト生成→実行はできるが、Rustコードの生成・コンパイル・テストまで自動化されていない。

```rust
// 新設: SelfHealingCodec パイプライン
pub enum CodeChange {
    Pattern { template_id: String, params: HashMap<String, String> },
    Native { rust_source: String, target_file: PathBuf },
}

pub struct VerifiedChange {
    pub change: CodeChange,
    pub check_result: cargo_check::Result,
    pub test_result: cargo_test::Result,
    pub rollback_id: String,  // 失敗時の自動ロールバック用
}
```

**実装項目**:
- [ ] `cargo_check` / `cargo_test` のプログラム的実行ラッパー（既存の一部流用）
- [ ] `VerifiedChange` — コンパイル+テスト通過までを1トランザクションに
- [ ] 失敗時の自動ロールバック（`rollback_to_event` 利用）
- [ ] 成功時の git commit 自動生成（`Co-Authored-By: Pharmakon`）

**ファイル**: `crates/core/src/orchestration/self_healing.rs`（新規）
**見積もり**: +400行, 3日

---

### 9-3: クリスタライゼーションの完成

**現状**: AOT Compiler (`aot.rs`) はバイナリシリアライズ＋Rustコード生成まで実装。実際のビルド統合・リリースサイクル自動化が未完了。

```rust
// 現状 (aot.rs): Rustコード生成まで
pub fn generate_crystallized_rust(pattern: &CompressedPattern) -> String { ... }

// 目標: ビルド統合まで
pub struct CrystallizationPipeline {
    pub generate: fn(&CompressedPattern) -> String,           // Rustコード生成
    pub insert_into_module: fn(&str, &str) -> Result<()>,     // モジュール注入
    pub verify_build: fn() -> Result<BuildReport>,            // cargo check
    pub promote: fn(&str) -> Result<()>,                      // 本番反映
}
```

**実装項目**:
- [ ] `CrystallizationPipeline` — コード生成→モジュール注入→ビルド確認→本番反映
- [ ] `crystallization_candidates()` — 現在の候補一覧表示コマンド
- [ ] `/crystallize <skill_id>` — 手動トリガー
- [ ] auto-crystallize: 週1回のバックグラウンドジョブ

**ファイル**: `crates/core/src/orchestration/aot.rs`（拡張）, `crates/core/src/orchestration/mod.rs`
**見積もり**: +300行, 2日

---

## 2. Phase 10: 実運用の完成

**コアテーゼ**: Phase 9 で「自己改善」を得た後、実運用に耐えるための最終仕上げ。

---

### 10-1: E2Eテスト基盤

**現状**: 62のユニットテスト＋MockModelを使った2つの統合テスト。E2Eテストが不足。

```rust
// 新設: e2e/ ディレクトリ
crates/core/tests/e2e/
├── mod.rs
├── basic_chat.rs           // chat() → response
├── tool_execution.rs       // tool_call → execute → result → response
├── multi_turn.rs           // user → model → tool → model → response
├── fallback.rs             // レートリミット→フォールバックモデル
├── rollback.rs             // ファイル変更→rollback→検証
└── budget.rs               // トークン予算超過→終了
```

**実装項目**:
- [ ] `MockModel` の機能拡張（tool_call応答、エラー注入、レイテンシ模擬）
- [ ] 一時ディレクトリ＋TestAgent builder
- [ ] 10+ のE2Eテストケース
- [ ] CI用の `cargo test --workspace --include-ignored` スクリプト

**ファイル**: `crates/core/tests/e2e/`（新規）
**見積もり**: +500行, 2日

---

### 10-2: データ永続化のスキーマバージョニング

**現状**: EventLog は JSONL、SnapshotStore は gzip圧縮ファイル、SQLiteにバージョン情報なし。

```rust
// EventLog にバージョンヘッダを追加
// Before:
{"id": 1, "timestamp": "...", "session_id": "...", "kind": {...}}

// After (1行目がバージョン):
# pharmakon-eventlog v1
{"id": 1, "timestamp": "...", "session_id": "...", "kind": {...}}
```

**実装項目**:
- [ ] EventLog: 先頭行にバージョンマーカー
- [ ] SnapshotStore: メタデータファイルにバージョンと総ストアサイズ
- [ ] SQLite: `PRAGMA user_version` 設定
- [ ] 移行スクリプト（古い形式を読み込み＋新しい形式で書き出し）

**ファイル**: `crates/core/src/event_log.rs`, `crates/core/src/snapshot_store.rs`, `crates/core/src/persistence.rs`
**見積もり**: +250行, 1日

---

### 10-3: tokio::spawn タスクの管理

**現状**: Dream Mode、snapshot pruning、background reflection が監視なしで spawn されている。

```rust
// 新設: BackgroundTaskRegistry
pub struct BackgroundTaskRegistry {
    tasks: Vec<(String, JoinHandle<()>)>,
}

impl BackgroundTaskRegistry {
    pub fn spawn<F>(&mut self, name: &str, f: F) where F: Future<Output=()> + Send + 'static;
    pub fn health_check(&self) -> Vec<(&str, bool)>;
    pub fn shutdown_all(&mut self) -> impl Future<Output=()>;
}
```

**実装項目**:
- [ ] `BackgroundTaskRegistry` 実装
- [ ] 既存の `tokio::spawn` 呼び出しを registry 経由に変更
- [ ] `/tasks` コマンドでバックグラウンドタスク一覧表示
- [ ] graceful shutdown

**ファイル**: `crates/core/src/tasks/detached.rs`, `crates/core/src/agent.rs`
**見積もり**: +200行, 1日

---

### 10-4: エラー処理の統一

**現状**: `let _ = ...` / `if let Err(e) = log::warn!(...)` / `?` が混在。基準が不明確。

**統一基準**:
```
データ喪失リスク → ? で呼び出し元に伝搬（呼び出し元でログ）
ベストエフォート   → if let Err(e) = log::warn!("{}", e)
イベント通知系     → let _ = ... （broadcastの受信者不在は正常）
```

**実装項目**:
- [ ] 全 crate の `let _ =` を監査（agent.rs は完了済み）
- [ ] tools / memory / gateway も同基準で統一
- [ ] clippy の `#[deny(unused_results)]` 追加を検討

**ファイル**: 全 crate
**見積もり**: +150行（変更）, 1日

---

## 3. DSGE経済層の拡張方針

**結論**: 「本格DSGEソルバ」の実装は不要。現状の閉形式解＋オンラインフィッティングで十分。

### 実装する価値がある拡張（優先順位順）

| # | 拡張 | 効果 | 見積もり |
|---|---|---|---|
| 1 | Beta-Binomial 逐次ベイズ更新: 成功率推定を閉形式で改善 | 成功率推定のロバスト性向上 | +80行, 半日 |
| 2 | 確率的ボラティリティ:  shadow_price に不確実性項 | トークン予算の保守的配分 | +60行, 半日 |
| 3 | ヒステリシス: レート制限後の冷却期間モデル化 | 実モデル挙動への追従改善 | +40行, 数時間 |

### 実装しないもの

- 摂動法（log-linearization）: データ不足、計算コストに見合わない
- ベイズ推定（MCMC）: 同上
- インパルス応答関数: ショックの特定が不可能

---

## 4. 優先順位マップ

```
Week 1-2 (P0 - 実運用の核):
  10-1: E2Eテスト基盤 (10+テスト)     → 信頼性の根幹
  10-3: tokio::spawn タスク管理      → 運用監視の前提

Week 3-4 (P1 - 自己改善の基盤):
  9-1: 自己コード修正の安全基盤       → 自己改善の前提
  9-2: コード生成・検証パイプライン   → 自己改善の実行
  10-4: エラー処理の統一             → 全crate統一

Week 5-6 (P2 - 自己改善の完成):
  9-3: クリスタライゼーション完成     → AOT統合
  10-2: データ永続化のバージョニング   → 将来の互換性

常時:
  DSGE拡張 (Beta-Binomial / 確率的ボラティリティ)
```

---

## 5. ファイル別実装規模見積もり

| ファイル | 操作 | 規模 | 内容 |
|---------|------|------|------|
| `tests/e2e/mod.rs` + 各テスト | 新規 | +500行 | E2Eテスト10+ |
| `orchestration/self_healing.rs` | 新規 | +400行 | コード生成→検証パイプライン |
| `orchestration/aot.rs` | 拡張 | +300行 | クリスタライゼーションパイプライン |
| `event_log.rs` | 変更 | +100行 | バージョンマーカー |
| `snapshot_store.rs` | 変更 | +100行 | メタデータ + バージョン |
| `persistence.rs` | 変更 | +50行 | PRAGMA user_version |
| `security/policy.rs` | 変更 | +150行 | SelfModificationPolicy |
| `tasks/detached.rs` | 拡張 | +200行 | BackgroundTaskRegistry |
| `agent.rs` | 変更 | +80行 | task registry統合 |
| `cognitive_economics.rs` | 拡張 | +140行 | Beta-Binomial / 確率的ボラティリティ |
| `dsge_integration.rs` | 拡張 | +40行 | ヒステリシス |
| 全crate | 変更 | +150行 | エラー処理統一監査 |

**総追加**: ~2,210行 / 正味変更: +1,860行
