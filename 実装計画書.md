# Pharmakon 実装計画書

**作成日**: 2026-05-11
**総コード規模**: ~13,600行 (Rust), 45+ファイル, 7 crate

---

## 完了状況

| # | 内容 | 状態 |
|---|---|---|
| Phase 0-8 | Foundation〜Cognitive Runtime 全機能実装 | ✅ |
| A1-A8, P0-P1 | コード品質改善10項目（テスト復活、ModelRouter抽出、応答消失修正等） | ✅ |
| W1-W5 | world.rs 修正（git clean削除、Bayesian修正、Condition型安全化等） | ✅ |
| Phase 11-13 | リソース最適化・Heartbeat 2.0・スケジューラ統一 | ✅ |
| R1 | グレースフルシャットダウン: shutdown_token + dream mode/heartbeat対応 | ✅ |
| R2 | イベントバス溢れ防止: try_send_event! マクロ導入 | ✅ |
| R3 | プロバイダパニック対策: モデル呼び出しをspawnでラップ | ✅ |
| R4 | Gateway HTTPS警告: 起動時にHTTPS推奨ログ表示 | ✅ |
| R5 | EventLog自動回転: 既存実装確認 + 公開定数化 | ✅ |
| R6 | マジックバリュー解消: SNAPSHOT_DID_NOT_EXIST 定数化 | ✅ |
| R7 | CodeActGateパース改善: shlex相当のコマンド解析 + コマンドバリエーション対応 | ✅ |
| R8 | コンフィグ排他制御: 一時ファイル+atomic rename による安全書き換え | ✅ |
| R9 | Clippy警告: common/plugin-sdk の from_str→from_str_tag リネーム | ✅ |
| R10 | Agent構造体グループ化: 9セクションにコメント分割（維持しやすい形に） | ✅ |

### 現状

```bash
cargo test --workspace   → 64 passed, 0 failed (core), 0 failed (workspace)
cargo clippy --workspace --all-targets → pharmakon-core 警告ゼロ、common/tools/plugin-sdk の主要警告も修正済み
```

---

## 残存課題（優先順位順）

### 凡例

```
🔥 CRITICAL: 実運用で必ず問題になる。放置厳禁。
⚠️  HIGH:    いつか顕在化する。遅くとも次のリリースまでに対応。
📌  MEDIUM:  あると良い。コードの品質・可読性向上。
🔧  LOW:     余裕があれば。
```

---

### 🔥 R1: グレースフルシャットダウンがない

**リスク**: `tokio::spawn` されたタスク（Dream Mode / InitiativeEngine / DetachedTaskRuntime監視 / SnapshotStore起動時prune）に停止機構がない。Agent が Drop されてもゾンビ化し、`Arc<StdMutex<RhaiSkillLibrary>>` のメモリリークが発生する。

**対象**:
- `agent.rs` L420-431: Dream Mode decayループ（`loop { sleep 300s; ... }`）
- `agent.rs` L220: SnapshotStore 起動時prune
- `heartbeat.rs` L23-53: HeartbeatManager の main loop
- `initiative.rs`: InitiativeEngineWorker
- `tasks/detached.rs`: タスク監視ループ

**対応**:
- [ ] Agent に `shutdown_token: Arc<AtomicBool>` を追加
- [ ] 全 spawn タスクが shutdown_token をチェックするよう変更
- [ ] Agent Drop 時のクリーンアップ処理（`Drop` impl または `shutdown()` メソッド）
- [ ] `perform_maintenance()` にゾンビタスクの回収処理を追加

**工数**: 半日

---

### 🔥 R2: イベントバス溢れの暗黙消失

**リスク**: `broadcast::channel(100)` の容量100。レシーバが詰まると `send()` は `Err(TrySendError)` を返すが、全て `let _ =` で握りつぶされている。高負荷時に UI 更新やエラー通知が静かに消え、ユーザーからは「動いていない」ように見える。

**対象**: agent.rs で約20箇所、model_router.rs、その他

```rust
// 全箇所で共通パターン
let _ = self.event_tx.send(Event::AgentThought { ... });
```

**対応**:
- [ ] 全 `let _ = event_tx.send(...)` を `log::warn!` または `if let Err(e) = ...` に変更
- [ ] チャネル溢れが発生した場合の縮退動作を定義（例: 重要イベントのみ再送）

**工数**: 30分

---

### ⚠️ R3: モデルプロバイダのパニックが Agent 全体をクラッシュさせる

**リスク**: `chat_on_session` の main loop（~900行の async fn）に `catch_unwind` がない。Gemini/OpenAI/DeepSeek/Anthropic プロバイダが内部パニックすると Agent 全体がクラッシュする。OpenAI SDK の不具合、Gemini の不正なレスポンスなどで顕在化。

**対応**:
- [ ] `chat_on_session` の main loop に `std::panic::catch_unwind` を追加（AssertUnwindSafe で wrap）
- [ ] プロバイダパニック時のフォールバック（次のモデルに切り替えて継続 or 安全なエラー応答を返す）
- [ ] テスト: `MockModel` でパニックを注入するテストケース追加

**工数**: 1時間

---

### ⚠️ R4: Gateway の平文 HTTP

**リスク**: Gateway の REST API が認証キーを持っているが、通信は平文 HTTP。LAN 内でも中間者攻撃でキーが漏れる。Web フロントエンドからも http://localhost:19999 で通信。

**対応**:
- [ ] 自己署名証明書の自動生成（初回起動時）
- [ ] HTTPS モード追加（`--tls` フラグ or config）
- [ ] 平文 HTTP モード維持（開発用、デフォルトは警告表示）

**工数**: 2時間

---

### 📌 R5: EventLog のローテーション機構がない

**リスク**: `~/.pharmakon/event_log/events.jsonl` に追記され続け、ローテーションがない。日次利用で数ヶ月後に数百MBに。ディスク容量逼迫の原因になる。

**対応**:
- [ ] JSONL の行数またはサイズ閾値で自動ローテーション
- [ ] 古いログの自動削除（デフォルト30日保持、設定可能）
- [ ] `pharmakon events prune --days 7` CLI コマンド

**工数**: 1時間

---

### 📌 R6: SnapshotStore の `"none"` マジックバリュー

**リスク**: `agent.rs` で `snapshot_before_id = Some("none".to_string())` とし、復元時に文字列比較で分岐している。`Option<String>` で表現できる状態を文字列の特殊値で表現しており、可読性が低い。

**対象**: `crates/core/src/agent.rs`

```rust
// Before
snapshot_before_id = Some("none".to_string());
if snapshot_id == "none" { /* ファイルが存在しなかった */ }

// After
enum SnapshotBefore { Snapshot(String), DidNotExist, NotTracked }
```

**対応**:
- [ ] `SnapshotBefore` enum を定義（`Snapshot(String)` / `DidNotExist` / `NotTracked`）
- [ ] 該当箇所の文字列比較をパターンマッチに置き換え

**工数**: 30分

---

### 📌 R7: CodeActGate のコマンドパースが脆い

**リスク**: `shell` コマンドの `ls` / `cat` / `grep` 検出が `starts_with` の単純一致。`ls -la`、`cat -n`、`grep -r` など実用的なコマンドをキャッチできない。

**対象**: `crates/core/src/orchestration/tool_scheduler.rs`

```rust
// Before: -la や -n オプションがあるとマッチしない
if cmd.starts_with("ls ") || cmd == "ls" { ... }
if cmd.starts_with("cat ") { ... }
```

**対応**:
- [ ] `shlex::split` でコマンドをパースし、ベースコマンド名を抽出してから判定
- [ ] `ls` → `ListDir`, `cat` / `head` / `tail` → `ReadFile`, `grep` / `rg` → `GrepFiles`
- [ ] `shlex` を `Cargo.toml` の依存に追加

**工数**: 1時間

---

### 📌 R8: コンフィグファイルの排他制御がない

**リスク**: `~/.pharmakon/config.json` を `serde_json` で読み書きしているがファイルロックがない。Gateway と CLI の同時書き込みで破損リスク。

**対応**:
- [ ] `fs2` または `file-lock` クレートでファイルロックを追加
- [ ] ロック取得失敗時のリトライ処理
- [ ] 設定の atomic 書き換え（一時ファイル→rename）

**工数**: 1時間

---

### 🔧 R9: Clippy 警告が common / plugin-sdk に残っている

**リスク**: `cargo clippy --workspace --all-targets -- -D warnings` が通らない。`from_str` 混乱警告、過剰引数警告が残っている。

**対応**:
- [ ] `pharmakon-common` の `from_str` → `FromStr` trait 実装か `parse_from_str` にリネーム
- [ ] `tool_scheduler.rs` の過剰引数関数を分割 or `#[allow(clippy::too_many_arguments)]`
- [ ] CI で `-D warnings` を有効化

**工数**: 30分

---

### 🔧 R10: Agent 構造体が依然として大きい

**リスク**: ModelRouter 抽出後も Agent に約50フィールド。機能追加ごとに肥大化し続ける。

**対応**:
- [ ] `ToolExecutor`: `registry` / `snapshot_store` / `event_log` / `policy_engine` / `tool_call_counts` を束ねる
- [ ] `SessionManager`: `session_states` / `session_id` / `history` / `working_memory` / `context_engine` を束ねる
- [ ] `MemoryManager`: `fact_memory` / `semantic_search` / `knowledge_nexus` / `graph_store` を束ねる

**工数**: 半日〜1日（リスク低いので計画的に）

---

## 優先順位マップ

```
Week 1:
  R2  イベントバス溢れ防止           (30分) 🔥
  R3  プロバイダパニック対策          (1h)   ⚠️
  R5  EventLog ローテーション         (1h)   📌
  R6  マジックバリュー解消            (30分) 📌

Week 2:
  R1  グレースフルシャットダウン       (半日) 🔥
  R7  CodeActGate パース改善          (1h)   📌

Week 3:
  R4  Gateway HTTPS                   (2h)   ⚠️
  R8  コンフィグ排他制御              (1h)   📌
  R9  Clippy警告クリア               (30分) 🔧

計画的:
  R10 Agent分割                      (半日〜1日) 🔧
```

---

## ファイル別修正規模

| ファイル | 内容 | 工数 |
|---|---|---|
| `agent.rs` | R1 シャットダウントークン追加+R2 イベントログ+R6 マジックバリュー+R3 catch_unwind | 2.5h |
| `model_router.rs` | R2 イベントログ | 15min |
| `event_log.rs` | R5 ローテーション | 1h |
| `tool_scheduler.rs` | R7 CodeActGate パース改善 | 1h |
| `heartbeat.rs` / `initiative.rs` | R1 shutdown_token チェック追加 | 30min |
| `automation/cron.rs` | R1 shutdown_token チェック追加 | 15min |
| `tasks/detached.rs` | R1 shutdown_token 連携 | 15min |
| `gateway/src/auth.rs` | R4 TLS対応 | 2h |
| `common/src/lib.rs` | R8 ファイルロック | 1h |
| `common/src/**` | R9 clippy警告修正 | 30min |
| Agent分割 (新規ファイル) | R10 ToolExecutor/SessionManager/MemoryManager | 半日〜1日 |
