# Pharmakon 修正計画書

作成日: 2026-05-11
元タスク: 自律動作テスト（初回セッション）
テスター: Claude Code

## 優先度マトリクス

| # | 問題 | 優先度 | 難易度 | 分類 |
|---|---|---|---|---|
| P0 | ツール実行後の応答消失 | HIGH | HIGH | コアループバグ |
| P1 | 時刻などリアルタイム情報をツール実行しない | HIGH | MEDIUM | プロンプト／ルーティング |
| P2 | Gateway API 401で全API利用不可 | HIGH | LOW | 設定／ドキュメント欠落 |
| P3 | Gemini provider警告（name field missing） | MEDIUM | MEDIUM | providerバグ |
| P4 | parallel_tool_test.rsが毎回失敗 | MEDIUM | LOW | テスト不備 |
| P5 | rustc警告クリーンアップ | LOW | LOW | コード品質 |

---

## P0: ツール実行後の応答消失

### 現象
`pharmakon agent --message "今の時刻を調べて"` などツール実行が必要なクエリで、エージェントが応答テキストを返さないことがある。trajectoryには空の `[TOOL]` ブロックだけが記録される。

### 仮説
Agentループにおけるtool result → レスポンス生成のフローで、ツール結果がモデルに正しく渡されていないか、LLMがツール結果を受けて発話を生成したがそのテキストがUIに戻る前に落ちている可能性。

### 要確認箇所
- `crates/core/src/agent.rs` — `chat()` / `process_message()` メソッド: ツール実行ループとレスポンス生成
- `crates/core/src/agent_router.rs` — ツール呼び出し後のメッセージ構築
- `crates/cli/src/main.rs:318-319` — chat()結果のprint
- `crates/core/src/providers/gemini.rs` — マルチターンツールコールのstreaming/non-streaming対応

### 調査ステップ
1. `pharmakon agent --message "dateを実行して"` で `RUST_LOG=debug` を有効化しログを取得
2. Agentループ内でtool call → tool result → model response の各ステップの入出力を確認
3. empty responseのケースと正常応答のケースの差分を特定

### 修正方針（仮説に基づく）
- ツール結果が空文字列の場合のハンドリング
- モデルレスポンスのテキスト抽出ロジックの堅牢化
- 必要に応じてagent loopにフォールバック応答の追加

---

## P1: リアルタイム情報をツール実行しない

### 現象
時間を聞かれた際、shell/dateツールを実行せずLLMのパラメトリック知識のみで回答する（「2026年5月9日」と誤答。正解は5月11日）。

### 要確認箇所
- `crates/core/src/agent.rs` — system prompt: ツール活用の指示が弱い可能性
- `crates/core/src/system_prompt.rs` — 現在のsystem prompt内容
- `crates/core/src/soul/` — 性格(soul)設定によりツール使用が抑制されている可能性
- `crates/core/src/providers/gemini.rs` — tool_config.modeが"AUTO"だが、temperatureやthinking設定の影響

### 修正方針
1. system promptに「現在時刻などリアルタイム情報が必要なクエリは必ずツール（shell/dateなど）を使う」旨を追加
2. または「時刻取得クエリ」をトリガーに自動でshell dateを実行するハンドラをagent.rsに追加
3. tool_config.modeを条件によって"ANY"に変更する戦略も検討

---

## P2: Gateway API 401で全API利用不可

### 現象
GatewayのAPIルート(`/api/v1/agent/chat` など)が `x-api-key` 認証を要求し、`PHARMAKON_CONTROL_API_KEY` が未設定のため401で全リクエストが拒否される。フロントエンド（静的ファイル）は閲覧可能。

### 要確認箇所
- `crates/gateway/src/auth.rs` — auth_middleware: キー未設定時の挙動

### 修正方針
auth_middlewareの挙動を以下に変更：

```
if キーが未設定:
    → 認証をスキップ（全許可） or
    → デフォルトキーを発行して初回セットアップ時に警告表示
```

またはREADME/GUIDEに `PHARMAKON_CONTROL_API_KEY` の設定手順を明記する。

### 実装案
- 選択肢A: auth_middlewareでキー未設定時は認証バイパス（開発向き）
- 選択肢B: onboarding(pharmakon onboard)でコントロールキー設定を促す（本番向き）

---

## P3: Gemini provider警告（name field missing）

### 現象
```
WARN pharmakon_core::providers::gemini] Tool message is missing the 'name' field, attempting fallback lookup for tool_call_id
```
CodeActツール使用時に常に表示される。

### 要確認箇所
- `crates/core/src/providers/gemini.rs:274` — function_responseのpartsにnameが含まれていない
- `crates/core/src/orchestration/codeact.rs` — CodeAct実行結果のtool_call構造

### 修正方針
Gemini APIが要求するfunction_responseのpart形式に`name`フィールドを追加。具体的には：

```rust
// gemini.rs map_to_gemini_content() 内
function_response: Some(GeminiFunctionResponse {
    name: tc.function.name.clone(),  // ← これを追加
    response: ...  // 既存
}),
```

GeminiFunctionResponse構造体の定義を確認し、`name`がoptionalなら追加、なければ構造体に追加する。

---

## P4: parallel_tool_test.rsが毎回失敗

### 現象
`parallel_tool_test.rs` が `#[ignore]` 属性を持たず、Ollama llama3.2が未インストールの環境で必ずテスト失敗する。

### 修正箇所
`crates/core/tests/parallel_tool_test.rs:129`

```rust
// Before:
#[tokio::test]
async fn test_parallel_tool_execution() {

// After:
#[tokio::test]
#[ignore = "requires Ollama with llama3.2"]
async fn test_parallel_tool_execution() {
```

---

## P5: rustc警告クリーンアップ

### 一覧
| ファイル | 警告 | 修正 |
|---|---|---|
| `crates/tools/src/terminal.rs:24` | フィールド `cwd` 未読 | 削除 or `_cwd` |
| `crates/core/src/hooks/token_economy.rs:11` | フィールド `threshold` 未読 | 削除 |
| `crates/core/src/orchestration/mcts.rs:12` | フィールド `agent_ref` 未読 | `_agent_ref` |
| `crates/core/src/orchestration/dsge_integration.rs:255` | 変数 `task` 未使用 | `_task` |
| `crates/core/src/orchestration/parallel.rs:352` | 変数 `deps` 未使用 | `_deps` |
| `crates/core/src/providers/gemini.rs:158` | フィールド `grounding_metadata` 未読 | `#[allow(dead_code)]` |
| `crates/core/src/messaging/auto_reply.rs:58` | 未処理のResult | `let _ = ...` 追加 |
| `crates/core/src/agent.rs:618` | `messages_to_send` 未読 | 削除 |
| `crates/core/tests/agent_logic.rs:3,89,103` | 未使用import / 不要mut | クリーンアップ |
| `crates/core/tests/multi_agent_test.rs:1` | 未使用import | 削除 |

### 実行コマンド
```bash
cargo fix --lib -p pharmakon-core --allow-dirty
cargo fix --tests -p pharmakon-core --allow-dirty
cargo clippy --workspace --all-targets --fix --allow-dirty
```

---

## 実行計画

### Phase 1: 即効（P4, P5）
所要時間: 5分
- parallel_tool_test.rsにignore追加
- rustc警告クリーンアップ（cargo fix → 手動残骸処理）

### Phase 2: Gateway認証改善（P2）
所要時間: 10分
- auth.rsにキー未設定時のバイパス処理追加
- またはREADMEに設定手順を追記

### Phase 3: Gemini provider修正（P3）
所要時間: 30分
- function_responseにnameフィールド追加
- 該当箇所でデバッグログ確認

### Phase 4: ツール実行問題調査（P0, P1）
所要時間: 2〜4時間
- デバッグログ収集と原因特定
- system prompt / agent loop 修正
- soul設定の影響確認

---

## 検証基準

各修正後に以下を確認：
```bash
# ビルド
cargo check --workspace --all-targets

# 全テスト（Ollama依存テストはignore前提）
cargo test --workspace --lib

# フォーマット
cargo fmt --all -- --check

# 実際の動作確認
pharmakon "今の時刻は？"  # P0/P1確認
pharmakon agent --message "Hello"  # P3警告確認

# Gateway
pharmakon gateway --port 19999 &
curl http://localhost:19999/api/v1/state  # P2確認
```
